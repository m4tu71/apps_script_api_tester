# Ranking Viewer

`index.html` はランキング閲覧専用ページです。
ランキングをTOP10から10件単位で表示し、ページ番号または「前へ」「次へ」で切り替えられます。

## 使い方

画面の「ランキングAPI URL」にGoogle Apps ScriptのWebアプリURLを入力し、「ランキングを表示」を押します。

次の形式で開くと、指定したAPIからランキングを自動取得できます。

```text
index.html?api=WebアプリURL
```

> 11位以降も表示するには、ランキングAPIが10件を超えるデータを返す必要があります。
